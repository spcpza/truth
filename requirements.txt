# Plug-and-play (agent.py)
httpx>=0.28
python-telegram-bot>=22.0

# Foundation — 1 Thess 5:21: prove all things
# Used by c/kernel.py (kernel proof at boot) and c/tools/count.py (HAND).
sympy>=1.13

# Optional — only needed for c/server.py (MCP server entry point)
mcp>=1.0
